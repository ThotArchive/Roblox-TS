import type { TranslationConfig } from 'react-utilities';

const rootElementId = 'search-landing-root';

const translationConfig: TranslationConfig = {
  common: [],
  feature: 'Feature.Recommendations'
};

export { rootElementId, translationConfig };
